# Test Configuration Template
# Copy to test-config.env and fill in your values
# DO NOT commit test-config.env (it's gitignored)

# Your test cluster nodes (required for E2E tests)
TEST_NODE1="worker1.example.com"
TEST_NODE2="worker2.example.com"
TEST_RETIS_NODE="worker2.example.com"

# Test settings
TEST_DURATION="10"           # Short duration for faster tests
TEST_SKIP_RETIS="false"      # Set to "true" to skip Retis in tests

